<div class="space-y-7"><div class="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-bold text-blue-600">Dashboard pelapor</p><h2 class="text-3xl font-black">Kelola laporan Anda</h2><p class="mt-2 text-slate-500">Laporan pending atau ditolak masih dapat diedit dan dihapus. Setelah disetujui, laporan menjadi arsip publik yang terkunci.</p></div><x-ui.button :href="route('complaints.create')" wire:navigate>＋ Buat Laporan</x-ui.button></div>
<div class="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><x-dashboard.stat-card label="Semua laporan" :value="$counts['all']" icon="▤"/><x-dashboard.stat-card label="Menunggu" :value="$counts['pending']" icon="◷"/><x-dashboard.stat-card label="Disetujui" :value="$counts['approved']" icon="✓"/><x-dashboard.stat-card label="Ditolak" :value="$counts['rejected']" icon="×"/></div>
<div class="panel grid gap-4 p-5 md:grid-cols-[1fr_220px]"><x-ui.input label="Cari laporan" wire:model.live.debounce.350ms="search" placeholder="Judul atau alamat"/><x-ui.select label="Status" wire:model.live="status"><option value="">Semua status</option><option value="pending">Menunggu</option><option value="approved">Disetujui</option><option value="rejected">Ditolak</option></x-ui.select></div>
<div class="space-y-4">@forelse($complaints as $complaint)<article class="panel overflow-hidden"><div class="grid md:grid-cols-[180px_1fr]"><x-media.tile :media="$complaint->primary_media" class="h-full min-h-48 rounded-none"/><div class="p-5"><div class="flex flex-wrap items-center gap-2"><x-badge.status :status="$complaint->status"/><x-badge.category :category="$complaint->categoryRecord" :label="$complaint->category_label" :color="$complaint->category_color"/><span class="text-xs text-slate-400">{{ $complaint->media->count() }} media</span></div><h3 class="mt-3 text-lg font-black">{{ $complaint->title }}</h3><p class="mt-2 line-clamp-2 text-sm text-slate-500">{{ $complaint->address_text }}</p>@if($complaint->status==='rejected'&&$complaint->rejected_reason)<div class="mt-3 rounded-xl bg-red-50 p-3 text-sm text-red-700 dark:bg-red-950/30 dark:text-red-300"><strong>Alasan penolakan:</strong> {{ $complaint->rejected_reason }}</div>@endif<div class="mt-4 flex flex-wrap gap-2"><x-ui.button :href="route('complaints.show',$complaint)" variant="secondary" wire:navigate>Detail</x-ui.button>@if($complaint->isEditableByReporter())<x-ui.button :href="route('complaints.edit',$complaint)" wire:navigate>Edit laporan</x-ui.button><x-ui.button variant="danger" wire:click="deleteComplaint({{ $complaint->id }})" wire:confirm="Hapus laporan ini beserta seluruh medianya?">Hapus</x-ui.button>@else<span class="inline-flex items-center rounded-xl bg-emerald-50 px-3 py-2 text-xs font-bold text-emerald-700 dark:bg-emerald-950/40 dark:text-emerald-300">Terkunci setelah disetujui</span>@endif</div></div></div></article>@empty<div class="panel p-12 text-center text-slate-500">Belum ada laporan.</div>@endforelse</div>{{ $complaints->links() }}</div>
