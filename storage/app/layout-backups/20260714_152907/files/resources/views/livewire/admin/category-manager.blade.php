<div class="space-y-7"><div><p class="text-sm font-bold text-blue-600">Konfigurasi sistem</p><h2 class="text-3xl font-black">Kelola kategori laporan</h2><p class="mt-2 text-slate-500">Tambah, ubah, urutkan, aktifkan, atau nonaktifkan kategori tanpa mengubah struktur database.</p></div>
<div class="grid gap-6 xl:grid-cols-[380px_1fr]"><form wire:submit="save" class="panel space-y-4 p-6"><h3 class="text-lg font-black">{{ $editingId?'Edit kategori':'Kategori baru' }}</h3><x-ui.input label="Nama" name="name" wire:model="name"/><x-ui.input label="Slug" name="slug" wire:model="slug" hint="Boleh kosong, akan dibuat dari nama."/><x-ui.textarea label="Deskripsi" name="description" wire:model="description" rows="3"></x-ui.textarea><div class="grid grid-cols-2 gap-3"><x-ui.input label="Warna" name="color" type="color" wire:model="color"/><x-ui.input label="Urutan" name="sortOrder" type="number" wire:model="sortOrder"/></div><x-ui.input label="Ikon teks" name="icon" wire:model="icon" hint="Contoh: road, trash, lightbulb."/><x-ui.checkbox label="Kategori aktif" wire:model="isActive"/><div class="flex gap-2"><x-ui.button type="submit">Simpan</x-ui.button>@if($editingId)<x-ui.button type="button" variant="secondary" wire:click="resetForm">Batal</x-ui.button>@endif</div></form>
<div class="panel overflow-hidden"><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-slate-100 text-left text-xs uppercase tracking-wide text-slate-500 dark:bg-slate-800"><tr><th class="p-4">Kategori</th><th class="p-4">Warna</th><th class="p-4">Laporan</th><th class="p-4">Status</th><th class="p-4 text-right">Aksi</th></tr></thead><tbody class="divide-y divide-slate-200 dark:divide-slate-800">@foreach($categories as $category)<tr><td class="p-4"><p class="font-black">{{ $category->name }}</p><p class="text-xs text-slate-500">{{ $category->slug }} · urutan {{ $category->sort_order }}</p></td><td class="p-4"><span class="inline-flex items-center gap-2"><span class="h-5 w-5 rounded-full" style="background:{{ $category->color }}"></span>{{ $category->color }}</span></td><td class="p-4">{{ $category->complaints_count }}</td><td class="p-4"><button wire:click="toggle({{ $category->id }})" class="rounded-full px-3 py-1 text-xs font-bold {{ $category->is_active?'bg-emerald-100 text-emerald-700':'bg-slate-100 text-slate-600' }}">{{ $category->is_active?'Aktif':'Nonaktif' }}</button></td><td class="p-4"><div class="flex justify-end gap-2"><x-ui.button variant="secondary" wire:click="edit({{ $category->id }})">Edit</x-ui.button><x-ui.button variant="danger" wire:click="remove({{ $category->id }})" wire:confirm="Hapus kategori ini? Jika sudah dipakai, kategori hanya akan dinonaktifkan.">Hapus</x-ui.button></div></td></tr>@endforeach</tbody></table></div></div></div></div>
